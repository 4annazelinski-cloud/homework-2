const PI = 3.14159;
//let radius = Number(prompt("Circl's radius is?").trim());
let radius = 5;
const area = Math.round((PI * (radius ** 2))*100)/100;

console.log(`A circle radius ${radius} has area ${area}`);
