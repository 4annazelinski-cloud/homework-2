prompt() - is getting input data from user returns as a string to computer.
"42"
Number() converts as an integer, parseInt() converts as an integer, parseFloat() converts as a floating number.
typeof returns data type of variable, typeof null; // returns "object".
.toUpperCase() - "TEST", .toLowerCase() - "test", .trim() - " test " => "test";
\n - new line, \t - spaces/tab;
..
Boolean=> true /false;
..
2=="2" - true; 2==="2" - false;
=== strict equality;
!== strict inequality;
&& - logical and, || logical or, ! logical not;
true && false => false, true || false => true;
!true => false;
water && air; fruit || vegetable;
floating numbers do not calculate strictly;
Math.round(1.234) returns 1;
let variables can be updated, const not;
SCREAMING_SNAKE_CASE;